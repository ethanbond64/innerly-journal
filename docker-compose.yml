version: '2'

services:

  server:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - '.env'
    volumes:
      - './backend:/backend'
      - '~/.innerly:/root/.innerly'
    ports:
      - '8000:8000'
